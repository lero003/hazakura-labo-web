---
import LogoMark from './LogoMark.astro';
import { siteNavigation } from '../data/site-navigation.js';
---

<nav id="nav-main" class="nav" aria-label="葉桜ラボの主要巡回路">
    <div class="nav-inner">
        <a href="#hero" class="nav-logo" id="nav-logo">
            <LogoMark />
            <span class="logo-text">葉桜ラボ</span>
        </a>
        <ul class="nav-links" id="nav-links">
            {siteNavigation.map((item) => (
                <li><a href={item.href}>{item.navLabel}</a></li>
            ))}
        </ul>
    </div>
</nav>
